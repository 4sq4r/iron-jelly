server:
  port: 8080
  servlet:
    context-path: /iron-jelly/api
spring:
  application:
    name: iron-jelly
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: iron_jelly
    password: i2n3cx13ans2612a
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect #test-github-action